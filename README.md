# zendesk_ticket_collection
Pulls Zendesk ticket data by created date, outputs data to a JSON file, outputs data to a csv file, and then creates basic metrics for number of tickets in selected time frame.
